[package]
name = "binetic-llama-cpp"
version.workspace = true
edition.workspace = true

[dependencies]
binetic-core = { path = "../binetic-core" }
libc = "0.2"
thiserror = "1"
tracing = "0.1"
once_cell = "1"
cstr = "0.1"

[build-dependencies]
cc = "1"
