[package]
name = "binetic-intent"
version = "0.1.0"
edition = "2021"

[dependencies]
binetic-core = { path = "../binetic-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
parking_lot = "0.12"

[lib]
name = "binetic_intent"
path = "src/lib.rs"